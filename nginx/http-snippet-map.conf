# Thêm MỘT LẦN vào khối http { } trong /etc/nginx/nginx.conf:
#   include /path/to/platform/nginx/http-snippet-map.conf;
#
# Hoặc copy nội dung map vào nginx.conf.

map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
